import { defineSchema } from "convex/server";

// Initial empty schema - will be populated in subsequent steps
export default defineSchema({
  // Tables will be added in following implementation steps
});